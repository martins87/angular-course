import { Component } from '@angular/core';

// decorator: a type that allows you to enhance your class
@Component({
    // the selector must be unique
    selector: 'app-server',
    templateUrl: './server.component.html'
})
export class ServerComponent {

}